/** ぼかしツールの最小幅（ピクセル） */
export const MIN_BLUR_WIDTH = 5
/** ぼかしツールの最大幅（ピクセル） */
export const MAX_BLUR_WIDTH = 300
/** ぼかしツールのデフォルト幅（ピクセル） */
export const DEFAULT_BLUR_WIDTH = 30
/** ぼかしツールのデフォルトハードネス（ぼかし強度） */
export const DEFAULT_BLUR_HARDNESS = 0.5
